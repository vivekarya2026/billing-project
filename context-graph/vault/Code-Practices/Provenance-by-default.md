---
type: CodePractice
description: "Every extracted field stores a confidence score and a bounding-box region referencing the source image. Nothing displayed without a source."
---

# Provenance by default

Every extracted field stores a confidence score and a bounding-box region referencing the source image. Nothing displayed without a source.
