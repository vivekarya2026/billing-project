---
type: CodePractice
description: "Every table has RLS enabled. A user only reads rows where user_id matches their JWT."
---

# Supabase Row-Level Security

Every table has RLS enabled. A user only reads rows where user_id matches their JWT.

## Applies to

- [[Supabase]]
